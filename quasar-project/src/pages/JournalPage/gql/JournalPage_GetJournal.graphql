query JournalPage_GetJournalById($id: uuid!) {
  journal_by_pk(id: $id) {
    author_id
    title
    content
    journal_users {
      user_id
    }
    comments {
      id
      created_at
      content
    }
  }
}
