mutation JournalPage_UpsertComment($comment: comment_insert_input!) {
  insert_comment_one(
    object: $comment
    on_conflict: { constraint: comment_pkey, update_columns: content }
  ) {
    id
  }
}
